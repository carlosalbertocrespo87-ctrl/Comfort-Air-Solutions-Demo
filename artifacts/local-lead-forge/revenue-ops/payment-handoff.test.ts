import{paymentBlocksHandoff,paymentNeedsAttention}from'./payment-exception.ts';import{handoffReady}from'./handoff.ts';
Deno.test('only verified payment can unblock handoff',()=>{for(const s of ['UNKNOWN','PENDING','FAILED','REFUNDED','DISPUTED'] as const)if(!paymentBlocksHandoff(s))throw new Error(`${s} incorrectly unblocked`);if(paymentBlocksHandoff('VERIFIED'))throw new Error('verified payment blocked');if(!paymentNeedsAttention('DISPUTED'))throw new Error('dispute hidden');});
Deno.test('handoff requires all delivery gates and zero P1',()=>{const p={paymentVerified:true,legalVerified:true,scopeLocked:true,clientDataComplete:true,deliveryOwnerAssigned:true,nextActionDefined:true,postPaymentPass:true,openP1:0};if(!handoffReady(p))throw new Error('valid handoff blocked');if(handoffReady({...p,postPaymentPass:false}))throw new Error('handoff without post-payment pass');if(handoffReady({...p,openP1:1}))throw new Error('handoff with P1');});
