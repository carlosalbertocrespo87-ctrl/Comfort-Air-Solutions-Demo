name: Bootstrap Air Company Demo (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo "This legacy one-off bootstrap is retired."
          echo "Use artifacts/prospect-configs/<slug>.json with the generic HVAC fast pipeline instead."
