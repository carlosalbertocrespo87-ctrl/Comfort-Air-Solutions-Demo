name: ASAP Demo CI

on:
  push:
    branches:
      - demo-asap-heating-air-current
    paths:
      - "artifacts/hvac-prospect-template/**"
      - "artifacts/prospect-configs/asap-heating-air.json"
      - "ASAP-DEMO-BUILD-SPEC.md"
      - ".github/workflows/asap-demo-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qa:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Apply ASAP configuration
        run: pnpm --dir artifacts/hvac-prospect-template run configure:asap

      - name: Typecheck ASAP demo
        run: pnpm --dir artifacts/hvac-prospect-template run typecheck

      - name: Build ASAP demo
        env:
          PORT: 3000
          BASE_PATH: /
        run: pnpm --dir artifacts/hvac-prospect-template run build

      - name: Static safety checks
        shell: bash
        run: |
          set -euo pipefail
          APP="artifacts/hvac-prospect-template/src/App.tsx"
          INDEX="artifacts/hvac-prospect-template/index.html"
          ROBOTS="artifacts/hvac-prospect-template/public/robots.txt"

          grep -q 'ASAP Heating & Air Conditioning' "$APP"
          grep -q 'Verified highlights' "$APP"
          grep -q 'noindex, nofollow, noarchive' "$INDEX"
          grep -q 'Disallow: /' "$ROBOTS"

          if grep -q 'PROSPECT HVAC COMPANY\|TARGET SERVICE AREA\|prospectcompany.com' "$APP"; then
            echo "Generic prospect placeholders remain in App.tsx" >&2
            exit 1
          fi

          if grep -q 'Marianne R\.\|Daniel K\.\|Priya S\.' "$APP"; then
            echo "Template fictional review names remain in App.tsx" >&2
            exit 1
          fi

          echo "ASAP static QA checks passed."

      - name: Install Chromium for render QA
        run: pnpm dlx playwright@1.55.0 install --with-deps chromium

      - name: Start built-site server
        shell: bash
        run: |
          set -euo pipefail
          python3 -m http.server 4173 --bind 127.0.0.1 --directory artifacts/hvac-prospect-template/dist/public > /tmp/asap-preview.log 2>&1 &
          echo $! > /tmp/asap-preview.pid
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/ >/dev/null; then
              echo "Built-site server ready."
              exit 0
            fi
            sleep 1
          done
          cat /tmp/asap-preview.log
          exit 1

      - name: Capture desktop and mobile renders
        run: |
          mkdir -p qa-artifacts
          pnpm dlx playwright@1.55.0 screenshot --device="Desktop Chrome" --full-page http://127.0.0.1:4173/ qa-artifacts/asap-desktop.png
          pnpm dlx playwright@1.55.0 screenshot --browser=chromium --viewport-size="390,844" --full-page http://127.0.0.1:4173/ qa-artifacts/asap-mobile.png

      - name: Upload render QA artifacts
        uses: actions/upload-artifact@v4
        with:
          name: asap-render-qa
          path: qa-artifacts/*.png
          if-no-files-found: error
